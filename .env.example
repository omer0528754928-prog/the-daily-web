# Environment variables

MONGODB_URI=mongodb://127.0.0.1:27017/the-daily-web
SESSION_SECRET=

# Development only: log in automatically as this user (e.g. reporter01) until real login exists
DEV_USERNAME=